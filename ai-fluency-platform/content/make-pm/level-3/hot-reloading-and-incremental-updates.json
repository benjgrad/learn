{
  "meta": {
    "title": "3.3 Hot Reloading and Incremental Updates",
    "description": "Understand the HMR protocol \u2014 filesystem watching, module dependency graph diffing, WebSocket delivery, and React Fast Refresh state preservation \u2014 and learn why Vite's native-ESM approach makes incremental preview updates sub-100ms while Webpack rebuilds can take 10+ seconds.",
    "level": "level-3",
    "slug": "hot-reloading-and-incremental-updates",
    "order": 3,
    "isCheckpoint": false,
    "isIndex": false
  },
  "blocks": [
    {
      "type": "predictPrompt",
      "prompt": "The agent edits 3 files in your React app. Full rebuild takes 8 seconds. The user expects to see changes in less than 1 second. How?\n\n---"
    },
    {
      "type": "markdown",
      "content": "## What You'll Learn\n\nBy the end of this module, you'll be able to explain how Hot Module Replacement works at each step of its pipeline \u2014 filesystem event to browser patch \u2014 and identify how the agent's file-write architecture (direct vs. API-based) adds latency that compounds across a multi-edit session."
    },
    {
      "type": "markdown",
      "content": "Vite's native-ESM architecture eliminates the bundling step entirely in dev mode, enabling sub-200ms preview updates by re-serving only the changed file \u2014 making AI agent edits feel instantaneous rather than batch. Without it, every agent edit triggers an 8-second wait \u2014 and in a world where agents make dozens of edits per session, that latency compounds into a broken product experience.\n\n**Full rebuild vs. incremental update**\n\nA full rebuild recompiles everything from scratch: reads all source files, resolves the full module graph, bundles all code, generates output assets, and serves the result. For a medium-sized React app, this takes 5-30 seconds with webpack and 1-5 seconds with Vite.\n\nAn incremental update detects which files changed, determines which parts of the module graph those files affect, recompiles only the affected modules, and sends only the changed modules to the browser. The browser patches the running application without a full page reload.\n\n**Hot Module Replacement (HMR)**\n\nHMR is the protocol that enables incremental updates. Here is how it works at a technical level:\n\n1. The dev server (Vite, Webpack Dev Server) watches the filesystem for changes\n2. When a file changes, the dev server computes a module dependency graph to determine which other modules depend on the changed file\n3. The dev server generates an HMR update: a small JavaScript payload containing only the changed module(s)\n4. The dev server sends this payload to the browser via WebSocket\n5. The browser's HMR runtime receives the payload and applies it: removes the old module from the module registry, evaluates the new module, and calls the module's HMR accept handler to re-render affected components\n\nThe key insight: the browser is not reloaded. The JavaScript runtime continues executing, and only the changed modules are swapped in.\n\n**React Fast Refresh**\n\nStandard HMR replaces modules but loses component state. If you have a form with user input, an HMR update wipes the input. React Fast Refresh extends HMR to preserve React component state across updates. It works by:\n\n- Identifying which React components changed\n- Re-rendering only those components\n- Preserving state for components that did not change\n- Resetting state only when the component's hook signature changes (a breaking change that cannot safely be preserved)\n\nFast Refresh is the standard for React development and is built into Vite's React plugin by default.\n\n**Why Vite is the dominant standard**\n\nTraditional bundlers (Webpack) process all modules through a bundling step at dev server start, creating a single (or chunked) bundle. This startup cost can be 10-60 seconds for large apps. File changes require rebundling the affected chunks.\n\nVite eliminates the dev bundling step entirely by leveraging native ES modules (ESM). Modern browsers can import JavaScript files directly \u2014 no bundling required. Vite's dev server serves source files as-is (with light transformation), and the browser's native module system handles the dependency graph. When a file changes, only that file is re-served. There is no bundle to recompute.\n\nThis makes Vite's HMR dramatically faster: the server does minimal work per file change, and only the single changed module is sent to the browser.\n\n---"
    },
    {
      "type": "calibrationCheck",
      "question": "A user is building a React app with Make. The agent modifies `utils/formatDate.ts`, which is imported by 12 different components. Will HMR successfully update the app without a full page reload? What determines the answer?",
      "answer": "*It depends on whether the module has an HMR accept handler and whether its exports changed in a way that breaks the module graph. If `formatDate.ts` is a pure utility module with no HMR accept handler, Vite will propagate the update up the dependency graph until it finds a component that does have an accept handler (which React Fast Refresh provides). As long as the components importing `formatDate.ts` are React components with Fast Refresh, the update should propagate cleanly. If a component does not have a Fast Refresh boundary, Vite falls back to a full page reload.*\n\n---"
    },
    {
      "type": "markdown",
      "content": "**The agent integration**\n\nWhen the Make agent writes a file to the preview environment's filesystem, the following must happen:\n\n1. The agent calls a file-write API (or directly writes to the virtual filesystem)\n2. The filesystem change triggers a file-system watch event (inotify on Linux, kqueue on macOS)\n3. The dev server's file watcher receives the event and identifies the changed file\n4. The dev server computes the HMR update and sends it to the browser via WebSocket\n5. The browser applies the update\n\nThe Platform PM's concern here: is the file-write API in the same process as the dev server, or does it cross a network boundary? If the agent writes files via a remote API call, the latency budget includes a network round-trip before the file system event is even triggered.\n\n**What breaks HMR**\n\nHMR is not magic \u2014 it has failure modes that trigger a full page reload:\n\n- **New imports**: If a changed file adds a new `import` statement, the module graph changes. Vite can handle some cases, but complex graph changes may require a full reload.\n- **Syntax errors**: A syntax error in the changed file prevents the module from being parsed. The HMR update fails, and the browser shows an error overlay.\n- **Non-component module changes**: HMR works best with React components. Changes to utility functions, constants, or configuration files may not have accept handlers, causing a full reload.\n- **Entry point changes**: Changes to the main entry point (main.tsx) typically require a full reload.\n\n**Latency breakdown**\n\nFor a typical Vite + React Fast Refresh setup, the update pipeline looks like:\n\n| Step | Latency |\n|---|---|\n| Agent writes file | 1ms |\n| Filesystem event delivered to dev server | 5-15ms |\n| Vite processes change, computes HMR update | 20-80ms |\n| HMR payload sent to browser via WebSocket | 2-10ms |\n| Browser receives and applies Fast Refresh | 30-80ms |\n| **Total** | **~60-185ms** |\n\nThis is well within the 500ms threshold for feeling instantaneous. The Platform PM's job is to ensure the infrastructure does not add latency to this pipeline \u2014 specifically, that the file-write API \u2192 filesystem event path is low-latency.\n\n---"
    },
    {
      "type": "markdown",
      "content": "## How to Think About This\n\nThink of HMR as a surgical patch, not a rebuild. The browser is a running program with state; HMR swaps individual modules into that running program without restarting it. The Platform PM's job is to ensure nothing in the infrastructure adds a fixed latency floor before the first step \u2014 because each agent edit in a session multiplies whatever overhead exists. A 100ms API round-trip before the filesystem event fires isn't a one-time cost; it's a tax on every single edit."
    },
    {
      "type": "calibrationCheck",
      "question": "The latency table shows the agent writes a file in 1ms and a filesystem event is delivered to the dev server in 5-15ms. An engineer proposes writing agent file changes via a remote API call that adds a 100ms network round-trip. What does this do to the user's HMR experience?",
      "answer": "It shifts the total update time from ~60-185ms to ~160-285ms \u2014 still under 500ms but meaningfully slower. More importantly, it changes the character of the experience: 60ms feels instantaneous (below the perceptual threshold for 'instant'); 260ms is noticeable as a small but perceptible delay. At the high end of normal variation (Vite processing a complex change), the total could reach 385ms, which starts feeling sluggish. The architectural concern isn't just the average case \u2014 it's that a remote API adds a latency floor that makes the worst case significantly worse. For an AI-assisted workflow where agents make dozens of edits per session, a 100ms fixed overhead per edit compounds into a noticeably less fluid experience."
    },
    {
      "type": "tryItYourself",
      "title": "Create a new Vite + React project (`npm create vite@latest`). Open a component file. Open DevTools Network tab, filter by WebSocket. Save the component file and observe the WebSocket message containing the HMR update. Try adding a new import \u2014 does the HMR update succeed or trigger a full reload?\n\n---",
      "solution": "After saving the component file, you should see a WebSocket frame arrive from the dev server containing a JSON message with a 'type' field \u2014 look for 'update' with a list of modules. The timing between your save and the WebSocket message (visible in DevTools) is your actual HMR pipeline latency, which should be 20\u2013100ms. When you add a new import, the behavior depends on whether the imported module is already in Vite's module graph. If it's a new package not yet installed, you'll see an error in the terminal (module not found). If it's an existing file in your project, Vite should handle it with a targeted update. If you add an import from a module with no HMR accept handler (like a plain utility function), you may see Vite fall back to a full page reload \u2014 the terminal will show 'page reload' instead of 'hmr update'. This fallback is the failure mode the module describes: complex graph changes trigger full reloads. Observe how quickly the page reloads in Vite versus what you'd expect from a webpack full rebuild \u2014 even the fallback is fast, which illustrates why Vite is the standard."
    },
    {
      "type": "explainBack",
      "prompt": "Explain to a PM from the agent team why it matters whether the agent writes files directly to the preview filesystem versus writing them via an API call. What is the practical user-experience consequence of the architectural difference?\n\n---"
    },
    {
      "type": "reflectPrompt",
      "questions": [
        "Should these agent edits be applied atomically (all at once) or sequentially (one file at a time)? Think of a product you've worked on where an update that touched multiple components could result in a broken intermediate state visible to users. How was that handled, and what was the tradeoff?",
        "Recall a time when a latency improvement in your product had a larger-than-expected impact on user behavior or engagement. What was the mechanism \u2014 why did the speed change matter beyond just 'feeling faster'?\n\n---"
      ]
    },
    {
      "type": "keyTakeaway",
      "content": "Hot Module Replacement \u2014 particularly Vite's native-ESM approach with React Fast Refresh \u2014 enables sub-200ms preview updates by recompiling and sending only changed modules to the browser, making AI agent edits feel instantaneous rather than batch.\n\n---"
    },
    {
      "type": "connectPrompt",
      "prompt": "HMR handles updates to existing dependencies. But what happens when the agent adds a brand new npm package? Before the preview can use it, it has to be installed \u2014 and `npm install` is famously slow. How do you make package installation fast in an ephemeral environment? That's next.\n\n---"
    }
  ],
  "reviewQuestions": [
    {
      "question": "In Vite's HMR pipeline, how does the dev server notify the browser that a file has changed?",
      "options": [
        "It triggers a full page reload, which forces the browser to re-request all assets",
        "It sends a small JavaScript payload containing only the changed module to the browser via WebSocket",
        "It writes an updated bundle to disk and the browser detects the change through a polling interval"
      ],
      "correctIndex": 1,
      "explanation": "Vite's HMR protocol uses a WebSocket connection to push a small payload \u2014 only the changed module \u2014 to the browser. The browser's HMR runtime applies the patch without a full reload, preserving application state.",
      "encourageCorrect": "That's it! The WebSocket delivery of a per-module patch is the mechanism that makes HMR feel instantaneous.",
      "encourageIncorrect": "HMR avoids a full reload entirely. It uses a persistent WebSocket connection: when a file changes, the dev server sends only that module's updated code to the browser as a small payload. No full reload, no polling, no full bundle rewrite."
    },
    {
      "question": "What is the total expected latency for a typical Vite + React Fast Refresh HMR update, from agent file write to the browser applying the patch?",
      "options": [
        "Approximately 60\u2013185 milliseconds",
        "Approximately 1\u20132 seconds",
        "Approximately 5\u201310 seconds"
      ],
      "correctIndex": 0,
      "explanation": "The module's latency table adds up filesystem event delivery (5\u201315ms), Vite processing (20\u201380ms), WebSocket transmission (2\u201310ms), and browser Fast Refresh application (30\u201380ms), for a total of roughly 60\u2013185ms \u2014 well under the 500ms threshold for feeling instantaneous.",
      "encourageCorrect": "Well done! 60\u2013185ms is the range from the latency table, and staying under 500ms is the key benchmark for feeling instantaneous.",
      "encourageIncorrect": "The module's latency table puts the full pipeline at approximately 60\u2013185ms \u2014 filesystem event (5\u201315ms), Vite processing (20\u201380ms), WebSocket (2\u201310ms), Fast Refresh (30\u201380ms). That's well under 500ms, which is the threshold for feeling instantaneous."
    },
    {
      "question": "React Fast Refresh preserves component state across HMR updates. Which specific type of change causes it to intentionally reset state instead?",
      "options": [
        "Any change to the component's JSX return statement",
        "A change to the component's hook signature \u2014 adding, removing, or reordering hooks",
        "A change to any prop type in the component's TypeScript interface"
      ],
      "correctIndex": 1,
      "explanation": "Fast Refresh is designed to preserve state wherever safely possible. The exception is when a component's hook signature changes \u2014 because the old state shape may be structurally incompatible with the new component after hooks are added, removed, or reordered.",
      "encourageCorrect": "Correct! Hook signature changes are the specific trigger for a state reset \u2014 any other edit typically preserves state.",
      "encourageIncorrect": "Fast Refresh doesn't reset state on every JSX or prop-type change. It specifically resets when the hook signature changes \u2014 for example, adding or removing a useState \u2014 because the old state shape can't be safely carried into a component with a different hook structure."
    }
  ]
}
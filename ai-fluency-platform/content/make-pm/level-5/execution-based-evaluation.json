{
  "meta": {
    "title": "5.2 Execution-Based Evaluation",
    "description": "Walk the five-step execution evaluation ladder \u2014 AST parsing, TypeScript build, runtime startup, headless render, functional test \u2014 and understand why each catches a failure class the previous step cannot, so skipping any rung leaves a category of broken apps shipping to users.",
    "level": "level-5",
    "slug": "execution-based-evaluation",
    "order": 2,
    "isCheckpoint": false,
    "isIndex": false
  },
  "blocks": [
    {
      "type": "predictPrompt",
      "prompt": "You have 500 eval prompts (\"build a todo app,\" \"add a login page,\" etc.). For each one, the agent generates code. How do you automatically determine if the code \"works\"?\n\n---"
    },
    {
      "type": "markdown",
      "content": "## What You'll Learn\n\nThis module walks the five-step execution evaluation ladder \u2014 from AST parsing through functional Playwright tests \u2014 and explains why each rung catches failure classes the previous one cannot. By the end, you'll be able to identify exactly which failure stage explains a given eval result and make informed build-vs-buy decisions for each step of the pipeline."
    },
    {
      "type": "markdown",
      "content": "Each step of the execution evaluation ladder catches a distinct class of failure that the previous step structurally cannot \u2014 which is why skipping any rung leaves a category of broken apps shipping to users, no matter how many steps below it pass. Static analysis catches some problems (syntax errors, type errors), but a surprising amount of code that looks syntactically correct produces broken apps at runtime. The Platform PM needs to understand the full execution eval pipeline to resource it appropriately and make build-vs-buy decisions about evaluation infrastructure.\n\n**The five-step execution evaluation ladder**\n\nEach step in this ladder catches a different class of failure. Stopping at any step leaves a category of failure undetected.\n\n**Step 1: Does it parse?**\n\nThe most basic check: does the code parse into a valid Abstract Syntax Tree (AST)? An AST parser reads the code's structure without executing it. Syntax errors \u2014 unclosed brackets, invalid JavaScript expressions, malformed JSX \u2014 fail at this step.\n\nTools: `acorn`, `@babel/parser`, TypeScript compiler in `--noEmit` mode. This step runs in milliseconds and can catch a large fraction of catastrophic failures (the kind where the agent generated obviously broken code).\n\n**Step 2: Does it build?**\n\nA TypeScript or bundler check goes further than AST parsing. TypeScript's type checker verifies that all types are consistent, all imports resolve, and all referenced variables exist. Vite's build step verifies that the module graph is valid and all assets can be resolved.\n\nThis catches: type mismatches that would cause runtime failures, missing imports, circular dependencies that cause build failures, incorrect configuration files (vite.config.ts, tsconfig.json).\n\nBuild checks require the actual build tooling to run against the generated code \u2014 not just parsing. This is a more expensive operation (5-30 seconds per eval case) but necessary.\n\n**Step 3: Does it run?**\n\nStarting the dev server and checking for runtime errors is the first execution check. The platform provisions a preview environment, injects the generated code, runs `npm install`, starts Vite, and monitors the process for immediate crashes.\n\nThis catches: runtime errors that are not detectable at build time (dynamic imports that fail, missing environment variables, runtime type errors in JavaScript), infinite loops that crash the process, out-of-memory errors in large apps.\n\n---"
    },
    {
      "type": "calibrationCheck",
      "question": "An app generates cleanly through steps 1 and 2 but crashes at step 3 with an \"environment variable not found\" error. What does this tell you about where the agent's failure lies, and is this the kind of failure that would show up in a HumanEval score?",
      "answer": "The failure is at the runtime boundary \u2014 the agent generated code that references an environment variable (e.g., `process.env.SUPABASE_URL`) without providing a fallback or configuration guidance. This is an infrastructure-context failure: the agent didn't account for the execution environment when generating the code. HumanEval wouldn't catch this at all \u2014 it tests self-contained function completion with no concept of deployment environment, configuration, or external dependencies. This is exactly the category of failure that makes execution-based evaluation necessary beyond static analysis."
    },
    {
      "type": "markdown",
      "content": "**Step 4: Does it render?**\n\nA headless browser (Playwright, Puppeteer) loads the app's URL and checks whether the page renders without errors. Checks include:\n\n- HTTP status code is 200 (not 404 or 500)\n- No JavaScript exceptions in the browser console\n- No error boundaries triggered (React's error fallback UI)\n- The page body contains visible content (not a blank white screen)\n- Critical UI elements are visible (headings, navigation, main content area)\n\nThis catches: React render errors that don't crash the server but produce a white screen, incorrect routing configuration, missing assets (fonts, images) that cause layout failures.\n\n**Step 5: Does it do the right thing?**\n\nTask-specific automated tests verify the app's actual functionality. For a \"todo app\" prompt:\n\n- Navigate to the app\n- Type text into the input field\n- Click the \"Add\" button\n- Assert that a new list item appears containing the typed text\n- Click the \"Delete\" button on the item\n- Assert that the item is removed from the list\n\nThese are Playwright end-to-end tests, written against the eval task specification. Each eval prompt must have a corresponding test script. This is the most expensive component of the eval system to build and maintain.\n\n**Infrastructure coupling**\n\nRunning steps 3-5 requires real environments: sandboxed compute (to run the app), a real browser (to render and interact with it), and network connectivity. This is the same infrastructure as the production preview environment.\n\nThe implication: the eval system is not a standalone testing tool. It is a scaled-up version of the preview infrastructure, running in batch mode against a corpus of eval prompts. The Platform PM must account for this when planning eval infrastructure capacity \u2014 running 500 evals simultaneously requires 500 preview environments.\n\n**Time and cost**\n\nA rough estimate for a single eval case:\n\n| Step | Time | Cost |\n|---|---|---|\n| Provision environment | 2-5s | Low |\n| Inject code + install deps | 10-30s | Low-moderate |\n| Build check | 5-30s | Low |\n| Start dev server | 3-10s | Low |\n| Headless browser render check | 5-15s | Moderate |\n| Playwright functional tests | 10-60s | Moderate |\n| **Total** | **35-150s** | |\n\nFor 500 eval cases run sequentially: 5-21 hours. Parallelized across 500 concurrent environments: 35-150 seconds total, but with the cost of provisioning 500 environments simultaneously.\n\nThis is a meaningful infrastructure cost. It shapes decisions about how often to run the full eval suite (not on every commit \u2014 perhaps nightly or pre-release) and whether to invest in faster execution (lighter sandboxes, cached dependency layers).\n\n## How to Think About This\n\nThe execution ladder is not a redundancy \u2014 it's a failure isolation system. Each step narrows the candidate space of what could be wrong. A parse failure tells you the agent produced malformed syntax; a build failure tells you the structure was valid but types or imports broke; a render failure tells you the runtime is broken but the code was structurally sound. This specificity is what makes the ladder operationally useful: when an eval fails, you know exactly which team and which fix is responsible, without manual investigation.\n\n**Flaky evals**\n\nNon-determinism is a serious problem for eval infrastructure. Sources of flakiness:\n\n- **Model non-determinism**: The agent generates slightly different code on each run, so the same eval prompt may pass one run and fail the next\n- **Infrastructure flakiness**: Network timeouts during npm install, container provisioning failures, browser crashes\n- **Test flakiness**: Playwright tests that depend on timing (animations, async data loading) and fail non-deterministically\n\nDistinguishing a real regression (the model got worse) from infrastructure flakiness (something in the environment failed) requires running each eval multiple times and using statistical thresholds rather than binary pass/fail.\n\n---"
    },
    {
      "type": "calibrationCheck",
      "question": "An eval run shows that 15% of \"todo app\" evals failed at step 4 (headless browser render check). Before concluding the agent regressed, what alternative explanations must be investigated?",
      "answer": "*Alternative explanations: (1) infrastructure flakiness \u2014 container provisioning failed for some environments, causing blank screen non-deterministically; (2) npm install failures \u2014 a dependency had a breaking change on npm and installs failed; (3) Playwright/headless browser issues \u2014 a browser binary update changed behavior; (4) eval test flakiness \u2014 the render check timeout is too aggressive for the current environment speed. Before concluding the agent regressed, rerun the 15% that failed. If the re-run passes, this is infrastructure flakiness, not a regression. If the failures are consistent, investigate the agent's output for the failing cases specifically.*\n\n---"
    },
    {
      "type": "tryItYourself",
      "title": "Write a simple Playwright test for a counter app (button increments a counter, button decrements, display shows current count). Run it against a locally built Vite app. Then deliberately introduce a bug (remove the onClick handler) and verify the test catches it. This is the pattern step 5 uses at scale.\n\n---",
      "solution": "Your test should navigate to the app URL, find the increment button by accessible role or test ID, click it three times, then assert the display shows \"3\". Then click the decrement button once and assert \"2\". With the onClick handler removed, the counter should remain at 0 \u2014 your assertions on values greater than 0 should fail with a message like \"expected 0 to equal 1\". If your test passed even after removing the handler, check that your selectors are correctly targeting the counter display element rather than accidentally matching static text. A well-written step-5 eval test catches exactly this class of failure \u2014 code that renders but doesn't respond to user interaction \u2014 which steps 1-4 would all pass."
    },
    {
      "type": "explainBack",
      "prompt": "The agent team proposes adding a new eval prompt category: \"integrate Stripe payments.\" Why does this category require significantly more infrastructure than a \"build a todo app\" eval, and what additional components does the platform need to support it?\n\n---"
    },
    {
      "type": "reflectPrompt",
      "questions": [
        "Think of a project where you had to ship without the testing infrastructure you wanted. What was the actual cost of that tradeoff \u2014 how long did it take for gaps to surface, and what form did they take?",
        "When you've worked on a platform or service that ran expensive automated tests, what signals convinced leadership to invest in that infrastructure? What argument finally landed, and what would you use that framing for again?\n\n---"
      ]
    },
    {
      "type": "keyTakeaway",
      "content": "Execution-based evaluation runs generated code through a five-step ladder \u2014 parse, build, run, render, function \u2014 each catching a different class of failure, and requires real preview environment infrastructure, making eval cost proportional to production infrastructure scale.\n\n---"
    },
    {
      "type": "connectPrompt",
      "prompt": "You can verify the app works. But does it look right? A functionally correct app with a broken layout, off-center heading, or clipped image is still a bad user experience. Catching visual failures requires a different evaluation approach \u2014 screenshot comparison and visual regression. That's next.\n\n---"
    }
  ],
  "reviewQuestions": [
    {
      "question": "Which of the following correctly names all five steps of the execution evaluation ladder in order?",
      "options": [
        "Lint, compile, test, deploy, monitor",
        "Parse (AST), build (TypeScript/bundler), run (dev server), render (headless browser), functional test (Playwright)",
        "Static analysis, unit test, integration test, end-to-end test, accessibility test"
      ],
      "correctIndex": 1,
      "explanation": "The module walks five specific steps: Step 1 (AST parsing \u2014 does it parse?), Step 2 (TypeScript/bundler build \u2014 does it build?), Step 3 (dev server startup \u2014 does it run?), Step 4 (headless browser render \u2014 does it render?), Step 5 (Playwright functional tests \u2014 does it do the right thing?).",
      "encourageCorrect": "That's the full ladder! Each step catches a failure class the previous one cannot, which is why all five rungs are necessary.",
      "encourageIncorrect": "The five steps are: AST parsing, TypeScript/bundler build, dev server startup, headless browser render check, and Playwright functional tests. Each rung catches a different failure class \u2014 stopping at any step leaves a category of broken apps undetected."
    },
    {
      "question": "Approximately how long does a single eval case take to run through the full execution ladder, according to the module's estimates?",
      "options": [
        "1-5 seconds",
        "35-150 seconds",
        "5-10 minutes"
      ],
      "correctIndex": 1,
      "explanation": "The module provides a cost table showing each step's time: provisioning (2-5s), code injection and npm install (10-30s), build check (5-30s), dev server startup (3-10s), headless browser render (5-15s), Playwright tests (10-60s). Total: 35-150 seconds per eval case.",
      "encourageCorrect": "Good recall! That 35-150 second range is why running 500 evals sequentially would take 5-21 hours \u2014 parallelization is necessary.",
      "encourageIncorrect": "The module's cost table adds up to 35-150 seconds per eval case. That's why 500 evals run sequentially would take 5-21 hours, making parallelization across concurrent environments necessary for timely release gates."
    },
    {
      "question": "An app passes Steps 1 (parse) and 2 (build) but crashes at Step 3 with an 'environment variable not found' error. Which failure class does this represent?",
      "options": [
        "A syntax error the AST parser should have caught",
        "A runtime failure \u2014 the agent generated code referencing an environment variable without providing a fallback or configuration guidance",
        "A type error the TypeScript compiler should have flagged at build time"
      ],
      "correctIndex": 1,
      "explanation": "The module's calibration check addresses this exact scenario: passing Steps 1 and 2 but failing at Step 3 means the failure is at the 'runtime boundary' \u2014 the agent didn't account for the execution environment. Syntax errors fail at Step 1; type errors at Step 2; this is a deployment-context failure only catchable at Step 3.",
      "encourageCorrect": "Right \u2014 this is a runtime boundary failure. The code is syntactically and structurally valid, but the agent didn't handle the deployment environment's configuration requirements.",
      "encourageIncorrect": "Syntax errors fail at Step 1 (parse) and type errors fail at Step 2 (build). Passing both but crashing at Step 3 means the failure is runtime \u2014 the agent generated code that depends on an environment variable without accounting for where it comes from in the actual execution environment."
    }
  ]
}